/**
 * Edit route uses the same screen as create; Inertia passes `configuration` props.
 * All strings: `system.code_configuration.create.*` in ./Create.jsx
 */
import Create from './Create';

export default Create;

