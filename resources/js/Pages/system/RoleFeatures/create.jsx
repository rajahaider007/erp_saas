/**
 * Create route currently reuses the role-features form from edit.jsx (project routing).
 * All strings: `system.role_features.edit.*` in ./edit.jsx
 */
import Edit from './edit';

export default Edit;
